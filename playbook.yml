---

- hosts: localhost
  roles:
    - common

    - brave
    - fish

    - development-tools
    - dotnet-core
    - docker
    - kubernetes

    - fonts
    - os-configuration
    
  vars_files: 
    - vars.yml